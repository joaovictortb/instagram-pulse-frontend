"use client";

import NflStudioApp from "@/src/studio/App";

/**
 * NFL Studio — editor de posts / carrossel (notícias Supabase + ESPN + templates).
 * Código em `src/studio/` (copiado de `nfl-studio-brasil`).
 */
export default function StudioPage() {
  return <NflStudioApp />;
}
